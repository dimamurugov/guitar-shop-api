{
  "name": "tilda-sync",
  "version": "1.0.0",
  "description": "Sync supplier CSV catalog to Tilda",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "sync": "node src/sync.js",
    "deploy": "npm install --production && node src/sync.js && node src/server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "sql.js": "^1.10.3",
    "csv-parse": "^5.5.0",
    "express": "^4.18.0",
    "node-cron": "^3.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
